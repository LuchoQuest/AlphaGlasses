<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.smartglass">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Inicia la actividad splash -->
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Navegacion para las diferentes ventanas (BottomNavigation)-->
        <!-- Inicia la actividad main, actividad principal -->
        <activity android:name=".MainActivity">
        </activity>

        <!-- Actividad pantalla_dispositivo -->
        <activity android:name=".PantallaDispositivo" />

        <!-- Actividad pantalla_llamadas -->
        <activity android:name=".PantallaLlamadas" />

        <activity android:name=".CustomProcess"/>

    </application>

</manifest>